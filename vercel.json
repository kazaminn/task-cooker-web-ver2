{
  "buildCommand": "pnpm build",
  "outputDirectory": "src/dist",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- src/ public/ index.html vite.config.ts tsconfig*.json package.json pnpm-lock.yaml",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
