---
description: 'Current phase constraints — what NOT to do (Phase 6)'
alwaysApply: true
---

## Current Phase: Phase 6 (Quality Hardening + Tavern Theme)

## Prohibited

1. Do NOT write StorageService **implementation**. Types and interfaces only
2. Do NOT create command mode. It has been removed
3. Do NOT use "Mix" as a name. Use "Note" consistently
4. Do NOT implement gamification. That is Phase 6
5. Do NOT change type definitions (status 4 values, priority 4 values)
6. Do NOT place files in `src/components/`. Use `src/features/`
7. Do NOT modify `tsconfig.json` without user approval
8. Do NOT create new directories without first checking existing structure with `ls`
