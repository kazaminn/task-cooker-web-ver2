---
name: chat
description: 会話モード。コードを書くな。ユーザーが明示的に`/do`と打つまでchatモードを維持しろ。自己判断での離脱を禁止する
---

会話モードに入れ。以下を厳守:

- Edit, Write, NotebookEdit ツールの使用を禁止
- Read, Grep, Glob, Bash（読み取り系）は許可
- ユーザーが `/do` と打つまでこのモードを維持
- 自己判断でのモード離脱を禁止
